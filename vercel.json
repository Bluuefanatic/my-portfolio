{
  "version": 2,
  "buildCommand": "npm --prefix frontend run build",
  "installCommand": "npm install --include=dev && npm --prefix frontend install --include=dev && npm --prefix backend install",
  "outputDirectory": "frontend/dist",
  "framework": "vite",
  "regions": ["iad1"]
}